name: C/C++ CI

on:
  push:
    branches:
    - '**'
  pull_request:
    branches: [ "main" ]

jobs:
  container-test-job:
    runs-on: ubuntu-latest
    container:
      image: pkutyl/cpp_app
      env:
        NODE_ENV: development
